export default function Admin() {
  return (
    <div className="glass rounded-3xl p-8">
      <div className="text-3xl font-extrabold">
        Admin <span className="text-[color:var(--gold2)]">Dashboard</span>
      </div>
      <div className="text-white/60 mt-2">
        Next phase: show /api/bookings/admin with status buttons + payment badges.
      </div>
    </div>
  );
}